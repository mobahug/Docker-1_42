FROM ft-rails:on-build
EXPOSE 3000
CMD ["rails", "s", "-b", "0.0.0.0", "-p" ,"3000"]

# Ruby on Rails (RoR) is a server-side application framework
# that is written in Ruby under MIT License.

# Rails is a model-view-controller (MVC) framework
# that provides default arrangements for a web service,
# database, and web service.
# It reassures and simplifies the usage of web standards,
# such as XML or JSON for the data transfer and CSS, JavaScript,
# and HTML for user interfacing.